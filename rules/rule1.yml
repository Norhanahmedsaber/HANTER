rules: 
  - id: insecure-deserialization 
    patterns: 
      - pattern: | 
          import pickle 
          pickle.loads($X) 
      - pattern: | 
          import pickle 
          pickle.loads($X)     
    message: "Avoid insecure deserialization using pickle.loads"