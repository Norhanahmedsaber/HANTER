id: tryStatement 
type: 1
patterns:
    - pattern: "
const x = () => {try {
    console.log(x)
  } catch(e) {
    throw anas
  } finally {
    console.log(5)
  }}"     
message: "Avoid insecure deserialization using pickle.loads"